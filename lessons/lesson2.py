####################################################################################
# UNPACKING (розпакування: tuples, lists, args, kwargs)
####################################################################################

################################## МНОЖИННЕ ПРИСВОЄННЯ ТА ОБМІН ЗМІННИМИ
# tuple1 = (1, 2)
# a, b = tuple1             # розпакування кортежу
# print(a, b)

# a, b = 5, 7
# a, b = b, a               # швидкий обмін значеннями місцями
# print(a, b)

################################## ЗБІР ЕЛЕМЕНТІВ ЧЕРЕЗ * (REST/SPREAD)
# l = [2, 4, 8, 1, 33, 9]

# a, b, *_ = l              # а=2, b=4, решта залишку ігнорується в '_'
# print(a, b)
# print(_)                  # [8, 1, 33, 9]

# _, b, *_, y, z = l        # пропуск першого, b=4, y=33, z=9
# print(b, y, z)

# *_, x, y, z = l           # ігнорування початку, беруться 3 останні елементи
# print(x, y, z)

################################## РОЗПАКУВАННЯ В АРГУМЕНТИ ФУНКЦІЇ (* та **)
# args = [1, 2, 4]
# dict_args = {"arg1": 4, "arg2": 8, "arg3": 7}

# def func(arg1, arg2, arg3):
#     print(arg1, arg2, arg3)

# func(*args)               # розпакування позиційних аргументів зі списку/кортежу
# func(*dict_args)          # розпакує тільки КЛЮЧІ словника: arg1, arg2, arg3
# func(**dict_args)         # розпакує КЛЮЧІ ТА ЗНАЧЕННЯ як im'я=значення


################################## Коротка шпаргалка розпакування
# a, b = (1, 2)             → розпакування кортежу / списку
# a, b = b, a               → обмін значень місцями
# head, *tail = [1, 2, 3]   → head = 1, tail = [2, 3] (збір залишку)
# _, b, *rest = lst         → пропуск елементів за допомогою _
# func(*list_or_tuple)      → розпакування елементів як позиційних аргументів
# func(**dictionary)        → розпакування словника як іменованих аргументів (kwargs)


####################################################################################
# FUNCTIONS & DECORATORS (функції та декоратори)
####################################################################################

################################## ПУСТА ФУНКЦІЯ
# def func():
#     # pass                # заглушка
#     ...                   # Ellipsis — те саме, що і pass

################################## ДЕКОРАТОРИ (DECORATORS)
# def decor(func):
#     def inner(*args, **kwargs):
#         print("************************************************")
#         func(*args, **kwargs)
#         print("************************************************")
#     return inner

# def decor2(func):
#     def inner(*args, **kwargs):
#         print("------------------------------------------------")
#         func(*args, **kwargs)
#         print("------------------------------------------------")
#     return inner

# @decor
# @decor2                   # декоратори застосовуються знизу вгору (спочатку decor2, потім decor)
# def greeting(name):
#     print(f"Hello, {name}")

# greeting("Olha")

# Ручний аналог синтаксису з @:
# greeting_decor = decor(decor2(greeting))
# greeting_decor("Max")


################################## Коротка шпаргалка функцій та декораторів
# pass / ...                → порожній блок (заглушка)
# @decorator                → синтаксичний цукор для декорування функції
# *args / **kwargs          → довільна кількість позиційних / іменованих аргументів
# decor(func)               → повертає нову обгортку (inner function)


####################################################################################
# SCOPE & CLOSURES (області видимості та замикання)
####################################################################################

################################## ГЛОБАЛЬНА ОБЛАСТЬ ВИДИМОСТІ ЗМІННИХ У ЦИКЛАХ
# for i in range(5):
#     print(i)

# print(i)                  # Змінні з циклів НЕ мають своєї області видимості і потрапляють у global scope!

################################## LOCALS ТА GLOBALS
# name = "Oleh"

# def a():
#     name = "Max"           # Локальна змінна
#     print(locals())       # {'name': 'Max'}

# print(globals())          # Словник з усіма глобальними змінними
# a()

################################## GLOBAL ТА NONLOCAL
# name = "Max"

# def outer():
#     name = "Olha"
#     def inner():
#         # nonlocal name   # Бере змінну з найближчого БАТЬКІВСЬКОГО (не глобального) scope і дозволяє її змінювати
#         global name     # Бере змінну з ГЛОБАЛЬНОГО scope
#         name = "Petja"
#         print(name)
#     inner()
#     print(name)

# outer()
# print(name)

################################## ЗАМИКАННЯ (CLOSURES)
# def counter():
#     count = 0
#     def inner():
#         nonlocal count    # Зберігає та змінює стан з зовнішньої функції
#         count += 1
#         return count
#     return inner

# counter_1 = counter()
# counter_2 = counter()

# print(counter_1())        # 1
# print(counter_2())        # 1
# print(counter_1())        # 2


################################## Коротка шпаргалка областей видимості
# globals() / locals()      → отримати словник глобальних / локальних змінних
# global x                  → вказує, що змінна х береться з глобальної області
# nonlocal x                → вказує, що змінна х береться з найближчого зовнішнього (обгортаючого) scope
# Closure (замикання)      → вкладена функція, яка пам'ятає стан змінних із зовнішньої функції


####################################################################################
# LAMBDA, MAP, FILTER & ITERATORS (анонімні функції та ітератори)
####################################################################################

################################## LAMBDA (АНОНІМНІ ФУНКЦІЇ)
# func = lambda a, b: a + b # Аналог (a, b) => a + b у JavaScript
# print(func(5, 6))

################################## MAP ТА FILTER
# l = [1, 2, 3, 4, 5, 6]

# m = map(lambda item: item**2, l)
# f = filter(lambda x: x % 2 == 0, l)

# for i in m:
#     print(i)

# ПОЯСНЕННЯ: Другий прохід по 'm' або 'f' нічого не виведе!
# Map та Filter повертають одноразові ІТЕРАТОРИ. Вони обробляють елементи "ліниво" (по одному)
# і після повного проходу "виснажуються" (вказують на кінець послідовності).
# Щоб використати дані кілька разів, збережіть їх у список: m_list = list(m)

################################## СОРТУВАННЯ ЗА КЛЮЧЕМ (KEY FUNCTION)
# users = [
#     {"name": "max", "age": 13},
#     {"name": "olga", "age": 1},
#     {"name": "petya", "age": 19},
#     {"name": "taras", "age": 33},
# ]

# users.sort(key=lambda x: len(x["name"])) # Сортування за довжиною імені
# print(users)


################################## Коротка шпаргалка Lambda, Map, Filter
# lambda args: expression   → однорядкова анонімна функція
# map(func, iterable)       → застосовує функцію до кожного елемента (повертає ітератор)
# filter(func, iterable)    → відбирає елементи, де func повертає True (повертає ітератор)
# list(map_obj)             → конвертує ітератор у список (робить його багаторазовим)
# list.sort(key=lambda...)  → кастомне сортування за результатом lambda-функції


####################################################################################
# TYPE HINTING (типізація даних)
####################################################################################

# Pyton — це мова з динамічною типізацією. Аннотації типів потрібні ДЛЯ РОЗРОБНИКА
# та інструментів (IDE, mypy), але інтерпретатор НЕ блокує виконання при помилці типів.

from typing import Any, TypedDict
from collections.abc import Callable

# a: Any = "Hello"
# b: int = "Hello"          # Python виконає цей код без помилок під час запуску!

# my_type = str | int | bool # Union тип (починаючи з Python 3.10)

################################## ТИПІЗАЦІЯ ФУНКЦІЙ БЕЗ ПОВЕРНЕННЯ
# def func1() -> None:
#     return

################################## TYPEDDICT (СТРУКТУРА СЛОВНИКА)
# User = TypedDict("User", {"name": str, "age": int, "house": int})
# users: list[User] = [{"name": "Max", "age": 15, "house": 44}]


################################## СКЛАДНА ТИПІЗАЦІЯ (CALLABLE)
def counter(a: str, b: int) -> Callable[[bool, list[str]], int]:
    count = 0

    def inner(f: bool, y: list[str]) -> int:
        nonlocal count
        count += 1
        return count

    return inner


################################## Коротка шпаргалка типізації
# x: int = 5                → аннотація простого типу
# x: str | int              → значення може бути str або int (Union)
# def fn() -> None          → функція нічого не повертає
# list[int] / dict[str, int]→ типізація колекцій
# TypedDict                 → завдання чіткого типу для ключів та значень словника
# Callable[[ArgTypes], Res] → типізація функцій (аргументи -> результат)
